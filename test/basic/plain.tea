var asyncFunc = function(name, callback) {
  callback(null, name === 'Bob');
};

var result = [];
if (asyncFunc('Bob', ~)) {
  result.push(true);
}
if (!asyncFunc('Jeff', ~)) {
  result.push(true);
}
